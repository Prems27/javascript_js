const all_new_heros = [...mavel_heros, ...dc_heros] // tis is called as spread operator
// as soon as we sread ten it won't be an array anymore instead it acts as an individual
console.log(all_new_heros);
 